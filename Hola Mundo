#include <iostream> // Incluimos la librería para entrada/salida

int main() {
    // La función main es el punto de entrada de todo programa en C++
    std::cout << "¡Hola, Mundo!" << std::endl; // Imprime el mensaje en la consola
    return 0; // Indica que el programa terminó exitosamente
}